[package]
name = "todo-cli"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "todo"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
tokio = { version = "1.35", features = ["full"] }
regex = "1.10"
